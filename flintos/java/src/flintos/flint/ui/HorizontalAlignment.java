package flint.ui;

public enum HorizontalAlignment {
    LEFT(0),
    CENTER(1),
    RIGHT(2);

    final int value;

    private HorizontalAlignment(int value) {
        this.value = value;
    }
}
