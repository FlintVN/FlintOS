package flint.ui;

public enum VerticalAlignment {
    TOP(0),
    CENTER(1),
    BOTTOM(2);

    final int value;

    private VerticalAlignment(int value) {
        this.value = value;
    }
}
